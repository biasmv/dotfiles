dotfiles
==============================================

installation
----------------------------------------------

Issue the following commands in your shell to install the dotfiles. The install script will create symlinks for the dotfiles.

  git clone http://github.com/biasmv/dotfiles
  cd dotfiles
  ./install.sh

